let word = ["Zeroth", "First", "Second", "Third", "Fourth"];

// Index numbers are inserted into index and iterates the whole array.
for (let index in word) {
  console.log(index);
  console.log(word[index]);
}
